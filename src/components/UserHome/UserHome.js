import React from "react";

export default class UserHome extends React.Component {
  render() {
    return (
      <div>
        <h1>This is the user's home page</h1>
        <p>The user registered because they see value</p>
        <p>
          Give the user more functionality for having registered and logged in
        </p>
      </div>
    );
  }
}
